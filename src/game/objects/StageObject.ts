import Container = Phaser.GameObjects.Container;

export class StageObject extends Container {
    constructor(scene, x?: number, y?: number) {
        super(scene, x, y);
    }
    update(time: number, delta: number) {

    }
}
